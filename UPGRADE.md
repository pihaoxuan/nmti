# NMTI V2.0 升级规划

## 一、核心功能升级

### 1.1 分层随机抽题（方案C）

**题库结构：**
| 维度 | 题库数量 | 每次抽取 |
|------|----------|----------|
| S(卷度) | 15道 | 6道 |
| L(忠诚度) | 15道 | 6道 |
| E(能量) | 15道 | 6道 |
| P(痛苦值) | 15道 | 6道 |
| 娱乐题 | 5道 | 1道 |
| **总计** | **65道** | **25道** |

**实现要点：**
- 每次测试随机打乱抽取顺序
- 结果计算需标准化（百分制）
- 支持种子随机（可复现）

---

### 1.2 深夜发疯版

#### 时间限制策略

**触发条件：**
- **23:00 - 03:00**：发疯版和正常版可自选
- **03:00 - 23:00**：仅正常版可用，发疯版锁定

**方案决策：**

| 时间段 | 可用模式 | 说明 |
|--------|----------|------|
| 03:00 - 23:00 | 仅正常版 | 白天时段，发疯版锁定 |
| 23:00 - 03:00 | 正常版/发疯版自选 | 深夜窗口，自由切换 |

**UI设计：**
```
白天首页（03:00-23:00）：
[开始测试 - 正常版]
[🌙 发疯版]  ← 锁定状态，点击提示“仅23:00-03:00可用”

深夜首页（23:00-03:00）：
[开始测试 - 正常版]
[🌙 发疯版]  ← 可用，高亮显示
```

**技术实现：**
```javascript
// 检测是否是发疯时段
function isInsaneHoursAvailable() {
  const hour = new Date().getHours()
  return (hour >= 23 || hour < 3)  // 23:00-02:59
}

// 获取按钮状态
function getInsaneButtonState() {
  if (isInsaneHoursAvailable()) {
    return { enabled: true, hint: '发疯版可用' }
  } else {
    return { enabled: false, hint: '仅23:00-01:00可用' }
  }
}
```

**设计理由：**
- 保持发疯版的稀缺性和仪式感
- 23:00-03:00 覆盖深夜加班人群和重度加班党
- 避免白天随意触发冲击体验

#### 发疯版改动

| 改动项 | 正常版 | 发疯版 |
|--------|--------|--------|
| 题目措辞 | 职场正经 | 发疯文学 |
| 选项风格 | 理性克制 | 情绪爆发 |
| 额外维度 | 无 | D(发疯指数) |
| 结果文案 | 职场建议 | 精神状态分析 |

**示例对比：**
```
正常版题目：
"领导布置了一个超出能力范围的任务，你："
A. 好的我试试！
B. 能不能再考虑一下...
C. 不行，我做不了

发疯版题目：
"领导布置了一个超出能力范围的任务，你："
A. 好的我试试！！！！！（然后边哭边做）
B. 呵呵，行
C. 我不想干了！！！（但还是干了）
```

---

### 1.3 反向测试

**玩法：**
1. 用户选择一个"目标人物"（同事/朋友/领导）
2. 回答"你觉得TA会怎么选"
3. 系统生成："你觉得TA是XX型牛马"
4. 可选：让对方也测试，显示匹配度

**页面流程：**
```
首页 → 选择模式（自测/反向测试）
     → 填写目标人物昵称
     → 回答问题（"你觉得TA会怎么选"）
     → 生成结果："在你眼里，XXX是SELP型牛马"
     → 可分享给TA
```

**传播预期：**
- 职场群疯传："测测你觉得谁最卷"
- 戏剧性：双方互相测，对比差异

#### 题目一致性设计

**核心问题：** 反向测试和同事自测时，题目是否需要保持一致？

**方案对比：**

| 方案 | 描述 | 优点 | 缺点 |
|------|------|------|------|
| **A. 不保证一致** | 反向测试和自测题目随机独立 | 实现简单，纯前端 | "我们做的不是同一套题"，对比不公平 |
| **B. 保证一致（URL种子）** | 反向测试生成带种子URL，同事打开用同一套题 | 公平对比，"我真的用这套题测你" | 需要URL参数传递 |
| **C. 记录题目ID** | 反向测试时记录题目，生成专属链接 | 精确对应 | 链接长，存储麻烦 |

**决策：方案B - URL种子**

**实现原理：**
```
1. 你做反向测试 → 系统生成随机种子（如：seed=abc123）
2. 结果页生成链接：nmti.com/?seed=abc123&from=同事A
3. 同事打开链接 → 用同一套题自测
4. 系统计算：你猜TA是SELP，TA自测是SLIA → 匹配度65%
```

**用户体验流程：**
```
你：测测你觉得同事小王是什么牛马
    ↓
结果：你觉得小王是SELP（卷王舔狗）
    ↓
分享链接给小王："来验证一下我猜得对不对"
    ↓
小王点开 → 做同一套题 → 结果：SLIA（卷王隐士）
    ↓
系统对比：你猜对了75%，维度只有E有偏差
```

**技术实现：**

```javascript
// 种子随机抽取题目
function seededRandom(seed) {
  // 使用种子生成伪随机序列
  // 相同种子 → 相同题目顺序
}

// 生成分享链接
function generateShareLink(seed, fromName) {
  return `${window.location.origin}?seed=${seed}&from=${encodeURIComponent(fromName)}`
}

// 匹配度计算
function calculateMatch(yourGuess, theirActual) {
  const match = {
    S: yourGuess.S === theirActual.S,
    L: yourGuess.L === theirActual.L,
    E: yourGuess.E === theirActual.E,
    P: yourGuess.P === theirActual.P,
  }
  return Object.values(match).filter(Boolean).length / 4 * 100
}
```

**优势：**
- ✅ 公平性：同一套题公平对比
- ✅ 传播性："我猜你是什么牛马，来验证"很有趣
- ✅ 无需后端：纯前端实现，种子存在URL里
- ✅ 隐私友好：不需要存储用户数据

---

## 二、安全加固方案

### 2.1 前端防护

| 措施 | 说明 | 实现难度 |
|------|------|----------|
| **代码混淆** | 压缩+混淆JS代码，增加逆向难度 | ⭐⭐ |
| **局部加密** | 题目数据加密存储，运行时解密 | ⭐⭐⭐ |
| **防调试** | 检测DevTools打开，禁用右键 | ⭐ |
| **请求签名** | 前端请求带签名，防篡改 | ⭐⭐⭐ |

**推荐组合：**
```
基础防护：代码压缩 + 禁用右键 + DevTools检测
进阶防护：题目数据Base64编码 + 前端解密
```

### 2.2 访问控制

| 措施 | 说明 | Side Effect |
|------|------|-------------|
| **频率限制** | 同一IP每分钟最多5次请求 | 可能误伤公共网络 |
| **UA检测** | 过滤明显爬虫UA | 可能误伤隐私浏览器 |
| **Cookie追踪** | 限制单用户测试次数 | 影响隐私 |

**推荐方案：**
```
轻量限制：每IP每分钟最多10次测试请求
不限制访问，只限制提交频率
```

### 2.3 数据保护

| 数据类型 | 保护措施 |
|----------|----------|
| 题目库 | 打包到前端，混淆加密 |
| 用户答案 | 不存储，仅前端计算结果 |
| 测试结果 | 可选存储，用于统计 |

**关键决策：**
```
纯前端计算 → 无数据库 → 无数据泄露风险
适合：静态部署（Cloudflare Pages）
```

### 2.4 防爬虫策略

| 措施 | 实现方式 |
|------|----------|
| robots.txt | 禁止爬取关键路径 |
| 动态题目ID | 每次刷新题目ID随机 |
| 无API暴露 | 纯前端，无后端API |

---

## 三、项目结构升级

```
nmti/
├── public/
│   └── favicon.svg
├── src/
│   ├── main.js
│   ├── App.vue
│   ├── style.css
│   ├── components/
│   │   ├── HomePage.vue          # 首页（选择模式）
│   │   ├── TestPage.vue          # 测试页
│   │   ├── ResultPage.vue        # 结果页
│   │   └── ModeSelector.vue      # 模式选择组件
│   ├── data/
│   │   ├── questions.js          # 题库（正常版+发疯版）
│   │   ├── questions-insane.js   # 发疯版题库
│   │   ├── types.js              # 16种类型
│   │   ├── calculator.js         # 计算逻辑
│   │   └── survival-guide.js     # 职场生存指南
│   ├── utils/
│   │   ├── random.js             # 随机抽取逻辑
│   │   ├── time.js               # 时间检测（发疯版）
│   │   └── security.js           # 安全防护
│   └── router/
│       └── index.js              # 路由配置
├── LICENSE
├── DESIGN.md
├── PLAN.md
└── UPGRADE.md                    # 本文档
```

---

## 四、实施步骤

## 四、实施步骤

### Phase 1：题库扩展 ✅ 完成

- [x] 65 道正常版题目（S/L/E/P 各 15 道 + 娱乐 5 道）
- [x] 25 道发疯版题目（S/L/E/P 各 6 道 + 娱乐 1 道）
- [x] 文件：`src/data/questions.js`, `src/data/questions-insane.js`

### Phase 2：分层随机抽题 ✅ 完成

- [x] `src/data/random.js` - Mulberry32 种子随机 + Fisher-Yates 洗牌
- [x] `src/data/calculator.js` - 标准化计算，支持 D 维度
- [x] 组件更新：TestPage.vue, ResultPage.vue, App.vue

### Phase 3：深夜发疯版 ✅ 完成

- [x] `src/utils/time.js` - 时间检测（23:00-03:00）
- [x] HomePage.vue - 发疯版按钮（锁定/解锁状态）
- [x] D 维度（发疯指数）评分系统
- [x] 发疯版题库已包含 D 维度分数

### Phase 4：反向测试 ✅ 完成

- [x] 首页添加"测测别人"按钮
- [x] 反向测试模态框（输入目标人物昵称）
- [x] TestPage 显示"你觉得TA会怎么选"提示
- [x] ResultPage 显示"你觉得XX是什么牛马"
- [x] 分享链接生成（带seed参数）
- [x] URL 参数解析（验证模式）
- [x] 验证模式UI提示

### Phase 5：安全加固 ✅ 完成

- [x] `src/utils/security.js` - 禁用右键、DevTools 检测、快捷键拦截
- [x] Base64 编解码工具
- [x] 控制台提示信息

---

## 五、技术决策

### 5.1 是否需要后端？

| 方案 | 优点 | 缺点 |
|------|------|------|
| **纯前端** | 部署简单、无服务器成本、无数据泄露 | 题目可被逆向获取 |
| 前端+轻后端 | 可统计、可追踪、可防护 | 增加复杂度 |

**决策：保持纯前端**

理由：
1. 题目本身不敏感（不是答案，是测试）
2. 纯前端部署成本最低
3. 混淆后逆向成本足够高

### 5.2 路由方案

| 方案 | 说明 |
|------|------|
| Vue Router | 支持URL路由，可分享特定页面 |
| 状态切换 | 当前方案，更简单 |

**决策：保持当前状态切换方案**

理由：
- 功能简单，不需要复杂路由
- URL干净，不暴露内部状态

---

## 六、预期效果

### 6.1 功能对比

| 功能 | V1.0 | V2.0 |
|------|------|------|
| 题目数量 | 25道固定 | 65道随机抽25道 |
| 测试模式 | 仅自测 | 自测 + 反向测试 |
| 时间感知 | 无 | 正常版/发疯版 |
| 安全防护 | 无 | 混淆+加密+防护 |

### 6.2 传播预期

| 场景 | 预期效果 |
|------|----------|
| 白天职场群 | "测测你是哪种牛马" |
| 深夜朋友圈 | "凌晨三点的我，真实了" |
| 同事互动 | "我觉得你肯定是SELP型牛马" |
| 重复测试 | 每次题目不同，可反复玩 |

---

## 七、下一步

**确认后开始 Phase 1：题库扩展**

我需要：
1. 确认题目风格（保持当前职场打工人基调）
2. 确认发疯版风格（多疯？发疯文学那种？）
3. 确认安全级别（轻量防护 vs 深度加固）

**或者你直接说"开搞"，我用当前风格开始写题库。**
